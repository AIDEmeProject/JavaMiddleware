[postgres]
url = postgresql://localhost:5432  # database connection string: driver://host:port
user = postgres   # your username (avoid using postgres user!)
password = postgres  # your password
key = row  # which column to be used as key or row
